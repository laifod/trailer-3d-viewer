import http from 'node:http';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../dist'),port=Number(process.env.PORT||8788);
http.createServer((req,res)=>{try{let url=decodeURIComponent(new URL(req.url,'http://localhost').pathname);if(url.endsWith('/'))url+='index.html';const file=path.resolve(root,'.'+url);if(file!==root&&!file.startsWith(root+path.sep)){res.writeHead(403);return res.end();}const ext=path.extname(file);res.setHeader('Content-Type',({'.html':'text/html; charset=utf-8','.css':'text/css','.mjs':'text/javascript','.svg':'image/svg+xml','.glb':'model/gltf-binary','.json':'application/json'}[ext]||'application/octet-stream'));res.setHeader('Cache-Control','no-cache');const stream=fs.createReadStream(file);stream.on('error',()=>{res.writeHead(404);res.end('Not found');});stream.pipe(res);}catch{res.writeHead(400);res.end();}}).listen(port,'127.0.0.1',()=>console.log('Preview http://127.0.0.1:'+port));
